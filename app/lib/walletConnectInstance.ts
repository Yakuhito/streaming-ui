import { WalletConnect } from './WalletConnect';

// Create a singleton instance
export const walletConnect = new WalletConnect();

// Export the instance as default as well for convenience
export default walletConnect; 